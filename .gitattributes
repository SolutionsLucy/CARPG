### Git LFS configuration for large mod JARs
# Track all JAR files in the mods folder with Git LFS to avoid large objects being stored in normal git history.
mods/*.jar filter=lfs diff=lfs merge=lfs -text
# Also track any top-level JARs (if present) and other large binaries you might add in the future
*.jar filter=lfs diff=lfs merge=lfs -text

# Optional: track common large asset types
*.zip filter=lfs diff=lfs merge=lfs -text
*.tgz filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
mods/*.jar !text !filter !merge !diff
*.jar !text !filter !merge !diff
*.zip !text !filter !merge !diff
*.tgz !text !filter !merge !diff
*.png !text !filter !merge !diff
